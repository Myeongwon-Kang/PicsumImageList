<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="picsum"
            type="com.kang6264.picsumimagelist.data.response.Picsum" />

        <variable
            name="actionHandler"
            type="com.kang6264.picsumimagelist.presentation.ui.main.ActionHandler" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:onClick="@{() -> actionHandler.openDetail(picsum)}">

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/image_download_url"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginStart="16dp"
            app:cardCornerRadius="8dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="fitXY"
                app:imageFromUrl="@{picsum.download_url}" />

        </com.google.android.material.card.MaterialCardView>

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:maxLines="2"
            android:textSize="18sp"
            app:author="@{picsum.author}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/image_download_url"
            app:layout_constraintTop_toTopOf="@+id/image_download_url" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>